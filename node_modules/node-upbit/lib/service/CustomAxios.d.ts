import { AxiosResponse } from "axios";
import { IAxiosProps } from "../@types/quotation";
import AuthorizationToken from "./AuthorizationToken";
export default class CustomAxios extends AuthorizationToken {
    constructor(UBIT_ACCESS_KEY?: string, UBIT_SECRET_KEY?: string);
    protected getData<T>({ method, url, }: IAxiosProps): Promise<AxiosResponse<T>>;
    protected getAuthData<T>({ method, url, }: IAxiosProps): Promise<AxiosResponse<T>>;
    protected getAuthParamData<T>({ method, url, params, }: IAxiosProps): Promise<AxiosResponse<T>>;
}
